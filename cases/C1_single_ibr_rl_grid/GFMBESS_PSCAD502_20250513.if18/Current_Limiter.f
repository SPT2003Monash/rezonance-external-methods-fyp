!=======================================================================
! Generated by: PSCAD v5.0.1.0
! Warning:  The content of this file is automatically generated.
!           Do not modify, as any changes made here will be lost!
!-----------------------------------------------------------------------
! Component     : Current_Limiter
! Description   : Current Limiter
!-----------------------------------------------------------------------


!=======================================================================

      SUBROUTINE Current_LimiterDyn(Imax_pu, Idqref_pu, Idqrefl_pu_,    &
     &   Enable, Priority, IdqLim_)

!---------------------------------------
! Standard includes 
!---------------------------------------

      INCLUDE 'nd.h'
      INCLUDE 'emtconst.h'
      INCLUDE 'emtstor.h'
      INCLUDE 's0.h'
      INCLUDE 's1.h'
      INCLUDE 's2.h'
      INCLUDE 's4.h'
      INCLUDE 'branches.h'
      INCLUDE 'pscadv3.h'
      INCLUDE 'fnames.h'
      INCLUDE 'radiolinks.h'
      INCLUDE 'matlab.h'
      INCLUDE 'rtconfig.h'

!---------------------------------------
! Function/Subroutine Declarations 
!---------------------------------------

      REAL    LIMIT         ! Hard Limiter

!---------------------------------------
! Variable Declarations 
!---------------------------------------


! Subroutine Arguments
      INTEGER, INTENT(IN)  :: Enable, Priority
      REAL,    INTENT(IN)  :: Imax_pu
      REAL,    INTENT(IN)  :: Idqref_pu(2)
      INTEGER, INTENT(OUT) :: IdqLim_(2)! Aliases: IdqLim
      REAL,    INTENT(OUT) :: Idqrefl_pu_(2)! Aliases: Idqrefl_pu

! Electrical Node Indices

! Control Signals
      INTEGER  IdLim, IqLim
      REAL     Id_ref_pu, Iq_ref_pu, RT_1, RT_2
      REAL     RT_3, RT_4, RT_5, Iq_min, Iq_max
      REAL     RT_6, RT_7, RT_8, RT_9, Id_min
      REAL     Id_max, RT_10, RT_11, Iqrefl_pu
      REAL     Iqref_pu, Idref_pu, Idrefl_pu

! Internal Variables
      LOGICAL  LVD1_1

! Indexing variables
      INTEGER ICALL_NO                            ! Module call num
      INTEGER ISTOI, ISTOF, IT_0                  ! Storage Indices
      INTEGER IPGB                                ! Control/Monitoring
      INTEGER SS                                  ! SS/Node/Branch/Xfmr


!---------------------------------------
! Local Indices 
!---------------------------------------

! Dsdyn <-> Dsout transfer index storage

      NTXFR = NTXFR + 1

      TXFR(NTXFR,1) = NSTOL
      TXFR(NTXFR,2) = NSTOI
      TXFR(NTXFR,3) = NSTOF
      TXFR(NTXFR,4) = NSTOC

! Define electric network subsystem number

      SS        = NODE(NNODE+1)

! Increment and assign runtime configuration call indices

      ICALL_NO  = NCALL_NO
      NCALL_NO  = NCALL_NO + 1

! Increment global storage indices

      ISTOI     = NSTOI
      NSTOI     = NSTOI + 6
      ISTOF     = NSTOF
      NSTOF     = NSTOF + 26
      IPGB      = NPGB
      NPGB      = NPGB + 6
      NNODE     = NNODE + 2
      NCSCS     = NCSCS + 0
      NCSCR     = NCSCR + 0

!---------------------------------------
! Transfers from storage arrays 
!---------------------------------------

      Id_ref_pu = STOF(ISTOF + 6)
      Iq_ref_pu = STOF(ISTOF + 7)
      RT_1     = STOF(ISTOF + 8)
      RT_2     = STOF(ISTOF + 9)
      RT_3     = STOF(ISTOF + 10)
      RT_4     = STOF(ISTOF + 11)
      RT_5     = STOF(ISTOF + 12)
      Iq_min   = STOF(ISTOF + 13)
      Iq_max   = STOF(ISTOF + 14)
      RT_6     = STOF(ISTOF + 15)
      RT_7     = STOF(ISTOF + 16)
      RT_8     = STOF(ISTOF + 17)
      RT_9     = STOF(ISTOF + 18)
      Id_min   = STOF(ISTOF + 19)
      Id_max   = STOF(ISTOF + 20)
      RT_10    = STOF(ISTOF + 21)
      RT_11    = STOF(ISTOF + 22)
      Iqrefl_pu = STOF(ISTOF + 23)
      Iqref_pu = STOF(ISTOF + 24)
      IdLim    = STOI(ISTOI + 5)
      IqLim    = STOI(ISTOI + 6)
      Idref_pu = STOF(ISTOF + 25)
      Idrefl_pu = STOF(ISTOF + 26)

! Array (1:2) quantities...
      DO IT_0 = 1,2
         Idqrefl_pu_(IT_0) = STOF(ISTOF + 3 + IT_0)
         IdqLim_(IT_0) = STOI(ISTOI + 2 + IT_0)
      END DO


!---------------------------------------
! Electrical Node Lookup 
!---------------------------------------


!---------------------------------------
! Configuration of Models 
!---------------------------------------

      IF ( TIMEZERO ) THEN
         FILENAME = 'Current_Limiter.dta'
         CALL EMTDC_OPENFILE
         SECTION = 'DATADSD:'
         CALL EMTDC_GOTOSECTION
      ENDIF
!---------------------------------------
! Generated code from module definition 
!---------------------------------------


! 50:[datatap] Scalar/Array Tap 
      Idref_pu = Idqref_pu(1)

! 60:[datatap] Scalar/Array Tap 
      Iqref_pu = Idqref_pu(2)

! 70:[square] Square 
      RT_2 = Idref_pu * Idref_pu

! 80:[square] Square 
      RT_1 = Imax_pu * Imax_pu

! 90:[square] Square 
      RT_6 = Iqref_pu * Iqref_pu

! 100:[sumjct] Summing/Differencing Junctions 
      RT_7 = + RT_1 - RT_6

! 110:[hardlimit] Hard Limiter 
!  Hard_Limit
      IF (0.0 .LE. 1.0e+308) THEN
        RT_9 = LIMIT(0.0, 1.0e+308, RT_7)
      ELSE
        RT_9 = RT_7
        IF (STORL(NSTORL) .EQV. .FALSE.) THEN
          CALL EMTDC_WARN(ICALL_NO,1364284238,1,"Lower limit is more tha&
     &n the upper limit, limits not enforced.")
          STORL(NSTORL) = .TRUE.
        ENDIF
      ENDIF
      NSTORL = NSTORL + 1
!

! 120:[sqrt] Square Root 
      LVD1_1 = STORL(NSTORL)
      IF (RT_9 .LT. 0.0) THEN
        RT_8 = 0.0
        IF (.NOT. LVD1_1) THEN
          CALL EMTDC_MESSAGE(ICALL_NO,1578467248,1,2,"A negative value i&
     &s detected as an input to the Square Root function. ")
          CALL EMTDC_MESSAGE(ICALL_NO,1578467248,1,-1,"Input is treated &
     &as 0.0.")
          STORL(NSTORL) = .TRUE.
        ENDIF
      ELSE
         RT_8 = SQRT(RT_9)
      ENDIF
      NSTORL = NSTORL + 1

! 130:[sumjct] Summing/Differencing Junctions 
      RT_3 = + RT_1 - RT_2

! 140:[hardlimit] Hard Limiter 
!  Hard_Limit
      IF (0.0 .LE. 1.0e+308) THEN
        RT_5 = LIMIT(0.0, 1.0e+308, RT_3)
      ELSE
        RT_5 = RT_3
        IF (STORL(NSTORL) .EQV. .FALSE.) THEN
          CALL EMTDC_WARN(ICALL_NO,578946114,1,"Lower limit is more than&
     & the upper limit, limits not enforced.")
          STORL(NSTORL) = .TRUE.
        ENDIF
      ENDIF
      NSTORL = NSTORL + 1
!

! 150:[sqrt] Square Root 
      LVD1_1 = STORL(NSTORL)
      IF (RT_5 .LT. 0.0) THEN
        RT_4 = 0.0
        IF (.NOT. LVD1_1) THEN
          CALL EMTDC_MESSAGE(ICALL_NO,626795706,1,2,"A negative value is&
     & detected as an input to the Square Root function. ")
          CALL EMTDC_MESSAGE(ICALL_NO,626795706,1,-1,"Input is treated a&
     &s 0.0.")
          STORL(NSTORL) = .TRUE.
        ENDIF
      ELSE
         RT_4 = SQRT(RT_5)
      ENDIF
      NSTORL = NSTORL + 1

! 160:[select] Two Input Selector 
      IF (Priority .EQ. RTCI(NRTCI)) THEN
         Iq_max = RT_4
      ELSE
         Iq_max = Imax_pu
      ENDIF
      NRTCI = NRTCI + 1
!

! 170:[gain] Gain Block 
!  Gain
      Iq_min = -1.0 * Iq_max

! 180:[select] Two Input Selector 
      IF (Priority .EQ. RTCI(NRTCI)) THEN
         Id_max = RT_8
      ELSE
         Id_max = Imax_pu
      ENDIF
      NRTCI = NRTCI + 1
!

! 190:[gain] Gain Block 
!  Gain
      Id_min = -1.0 * Id_max

! 200:[hardlimit] Hard Limiter 
!  Hard_Limit
      IF (Iq_min .LE. Iq_max) THEN
        Iqrefl_pu = LIMIT(Iq_min, Iq_max, Iqref_pu)
      ELSE
        Iqrefl_pu = Iqref_pu
        IF (STORL(NSTORL) .EQV. .FALSE.) THEN
          CALL EMTDC_WARN(ICALL_NO,2058135246,1,"Lower limit is more tha&
     &n the upper limit, limits not enforced.")
          STORL(NSTORL) = .TRUE.
        ENDIF
      ENDIF
      NSTORL = NSTORL + 1
!

! 210:[select] Two Input Selector 
      IF (Enable .EQ. RTCI(NRTCI)) THEN
         Iq_ref_pu = Iqrefl_pu
      ELSE
         Iq_ref_pu = Iqref_pu
      ENDIF
      NRTCI = NRTCI + 1
!

! 220:[sumjct] Summing/Differencing Junctions 
      RT_11 = - Iqrefl_pu + Iqref_pu

! 230:[range] Range Comparator 
!  Band_Compare
      IF (RT_11 .LT. 0.0) THEN
         IqLim = NINT(1.0)
      ELSE IF (RT_11 .LE. 0.0) THEN
         IqLim = NINT(0.0)
      ELSE
         IqLim = NINT(1.0)
      ENDIF
!

! 240:[hardlimit] Hard Limiter 
!  Hard_Limit
      IF (Id_min .LE. Id_max) THEN
        Idrefl_pu = LIMIT(Id_min, Id_max, Idref_pu)
      ELSE
        Idrefl_pu = Idref_pu
        IF (STORL(NSTORL) .EQV. .FALSE.) THEN
          CALL EMTDC_WARN(ICALL_NO,1743385651,1,"Lower limit is more tha&
     &n the upper limit, limits not enforced.")
          STORL(NSTORL) = .TRUE.
        ENDIF
      ENDIF
      NSTORL = NSTORL + 1
!

! 250:[sumjct] Summing/Differencing Junctions 
      RT_10 = - Idrefl_pu + Idref_pu

! 260:[range] Range Comparator 
!  Band_Compare
      IF (RT_10 .LT. 0.0) THEN
         IdLim = NINT(1.0)
      ELSE IF (RT_10 .LE. 0.0) THEN
         IdLim = NINT(0.0)
      ELSE
         IdLim = NINT(1.0)
      ENDIF
!

! 270:[select] Two Input Selector 
      IF (Enable .EQ. RTCI(NRTCI)) THEN
         Id_ref_pu = Idrefl_pu
      ELSE
         Id_ref_pu = Idref_pu
      ENDIF
      NRTCI = NRTCI + 1
!

! 280:[datamerge] Merges data signals into an array 
      IdqLim_(1) = IdLim
      IdqLim_(2) = IqLim

! 290:[datamerge] Merges data signals into an array 
      Idqrefl_pu_(1) = Id_ref_pu
      Idqrefl_pu_(2) = Iq_ref_pu

! 320:[pgb] Output Channel 'IqLim'

      PGB(IPGB+1) = REAL(IqLim)

! 330:[pgb] Output Channel 'IdLim'

      PGB(IPGB+2) = REAL(IdLim)

! 340:[pgb] Output Channel 'Iq_ref_pu'

      PGB(IPGB+3) = Iq_ref_pu

! 350:[pgb] Output Channel 'Id_max'

      PGB(IPGB+4) = Id_max

! 360:[pgb] Output Channel 'Id_ref_pu'

      PGB(IPGB+5) = Id_ref_pu

! 370:[pgb] Output Channel 'Iq_max'

      PGB(IPGB+6) = Iq_max

!---------------------------------------
! Feedbacks and transfers to storage 
!---------------------------------------

      STOF(ISTOF + 1) = Imax_pu
      STOI(ISTOI + 1) = Enable
      STOI(ISTOI + 2) = Priority
      STOF(ISTOF + 6) = Id_ref_pu
      STOF(ISTOF + 7) = Iq_ref_pu
      STOF(ISTOF + 8) = RT_1
      STOF(ISTOF + 9) = RT_2
      STOF(ISTOF + 10) = RT_3
      STOF(ISTOF + 11) = RT_4
      STOF(ISTOF + 12) = RT_5
      STOF(ISTOF + 13) = Iq_min
      STOF(ISTOF + 14) = Iq_max
      STOF(ISTOF + 15) = RT_6
      STOF(ISTOF + 16) = RT_7
      STOF(ISTOF + 17) = RT_8
      STOF(ISTOF + 18) = RT_9
      STOF(ISTOF + 19) = Id_min
      STOF(ISTOF + 20) = Id_max
      STOF(ISTOF + 21) = RT_10
      STOF(ISTOF + 22) = RT_11
      STOF(ISTOF + 23) = Iqrefl_pu
      STOF(ISTOF + 24) = Iqref_pu
      STOI(ISTOI + 5) = IdLim
      STOI(ISTOI + 6) = IqLim
      STOF(ISTOF + 25) = Idref_pu
      STOF(ISTOF + 26) = Idrefl_pu

! Array (1:2) quantities...
      DO IT_0 = 1,2
         STOF(ISTOF + 1 + IT_0) = Idqref_pu(IT_0)
         STOF(ISTOF + 3 + IT_0) = Idqrefl_pu_(IT_0)
         STOI(ISTOI + 2 + IT_0) = IdqLim_(IT_0)
      END DO


!---------------------------------------
! Transfer to Exports
!---------------------------------------
      !Idqrefl_pu_ is output
      !IdqLim_  is output

!---------------------------------------
! Close Model Data read 
!---------------------------------------

      IF ( TIMEZERO ) CALL EMTDC_CLOSEFILE
      RETURN
      END

!=======================================================================

      SUBROUTINE Current_LimiterOut()

!---------------------------------------
! Standard includes 
!---------------------------------------

      INCLUDE 'nd.h'
      INCLUDE 'emtconst.h'
      INCLUDE 'emtstor.h'
      INCLUDE 's0.h'
      INCLUDE 's1.h'
      INCLUDE 's2.h'
      INCLUDE 's4.h'
      INCLUDE 'branches.h'
      INCLUDE 'pscadv3.h'
      INCLUDE 'fnames.h'
      INCLUDE 'radiolinks.h'
      INCLUDE 'matlab.h'
      INCLUDE 'rtconfig.h'

!---------------------------------------
! Function/Subroutine Declarations 
!---------------------------------------


!---------------------------------------
! Variable Declarations 
!---------------------------------------


! Electrical Node Indices

! Control Signals

! Internal Variables

! Indexing variables
      INTEGER ICALL_NO                            ! Module call num
      INTEGER ISTOL, ISTOI, ISTOF, ISTOC          ! Storage Indices
      INTEGER SS                                  ! SS/Node/Branch/Xfmr


!---------------------------------------
! Local Indices 
!---------------------------------------

! Dsdyn <-> Dsout transfer index storage

      NTXFR = NTXFR + 1

      ISTOL = TXFR(NTXFR,1)
      ISTOI = TXFR(NTXFR,2)
      ISTOF = TXFR(NTXFR,3)
      ISTOC = TXFR(NTXFR,4)

! Define electric network subsystem number

      SS        = NODE(NNODE+1)

! Increment and assign runtime configuration call indices

      ICALL_NO  = NCALL_NO
      NCALL_NO  = NCALL_NO + 1

! Increment global storage indices

      NPGB      = NPGB + 6
      NNODE     = NNODE + 2
      NCSCS     = NCSCS + 0
      NCSCR     = NCSCR + 0

!---------------------------------------
! Transfers from storage arrays 
!---------------------------------------



!---------------------------------------
! Electrical Node Lookup 
!---------------------------------------


!---------------------------------------
! Configuration of Models 
!---------------------------------------

      IF ( TIMEZERO ) THEN
         FILENAME = 'Current_Limiter.dta'
         CALL EMTDC_OPENFILE
         SECTION = 'DATADSO:'
         CALL EMTDC_GOTOSECTION
      ENDIF
!---------------------------------------
! Generated code from module definition 
!---------------------------------------


!---------------------------------------
! Feedbacks and transfers to storage 
!---------------------------------------



!---------------------------------------
! Close Model Data read 
!---------------------------------------

      IF ( TIMEZERO ) CALL EMTDC_CLOSEFILE
      RETURN
      END

!=======================================================================

      SUBROUTINE Current_LimiterDyn_Begin()

!---------------------------------------
! Standard includes 
!---------------------------------------

      INCLUDE 'nd.h'
      INCLUDE 'emtconst.h'
      INCLUDE 's0.h'
      INCLUDE 's1.h'
      INCLUDE 's4.h'
      INCLUDE 'branches.h'
      INCLUDE 'pscadv3.h'
      INCLUDE 'radiolinks.h'
      INCLUDE 'rtconfig.h'

!---------------------------------------
! Function/Subroutine Declarations 
!---------------------------------------


!---------------------------------------
! Variable Declarations 
!---------------------------------------


! Subroutine Arguments

! Electrical Node Indices

! Control Signals

! Internal Variables

! Indexing variables
      INTEGER ICALL_NO                            ! Module call num
      INTEGER SS                                  ! SS/Node/Branch/Xfmr


!---------------------------------------
! Local Indices 
!---------------------------------------


! Define electric network subsystem number

      SS        = NODE(NNODE+1)

! Increment and assign runtime configuration call indices

      ICALL_NO  = NCALL_NO
      NCALL_NO  = NCALL_NO + 1

! Increment global storage indices

      NNODE     = NNODE + 2
      NCSCS     = NCSCS + 0
      NCSCR     = NCSCR + 0

!---------------------------------------
! Electrical Node Lookup 
!---------------------------------------


!---------------------------------------
! Generated code from module definition 
!---------------------------------------


! 50:[datatap] Scalar/Array Tap 

! 60:[datatap] Scalar/Array Tap 

! 70:[square] Square 

! 80:[square] Square 

! 90:[square] Square 

! 100:[sumjct] Summing/Differencing Junctions 

! 110:[hardlimit] Hard Limiter 

! 120:[sqrt] Square Root 

! 130:[sumjct] Summing/Differencing Junctions 

! 140:[hardlimit] Hard Limiter 

! 150:[sqrt] Square Root 

! 160:[select] Two Input Selector 
      RTCI(NRTCI) = 1
      NRTCI = NRTCI + 1

! 170:[gain] Gain Block 

! 180:[select] Two Input Selector 
      RTCI(NRTCI) = 0
      NRTCI = NRTCI + 1

! 190:[gain] Gain Block 

! 200:[hardlimit] Hard Limiter 

! 210:[select] Two Input Selector 
      RTCI(NRTCI) = 1
      NRTCI = NRTCI + 1

! 220:[sumjct] Summing/Differencing Junctions 

! 230:[range] Range Comparator 

! 240:[hardlimit] Hard Limiter 

! 250:[sumjct] Summing/Differencing Junctions 

! 260:[range] Range Comparator 

! 270:[select] Two Input Selector 
      RTCI(NRTCI) = 1
      NRTCI = NRTCI + 1

! 280:[datamerge] Merges data signals into an array 

! 290:[datamerge] Merges data signals into an array 

! 320:[pgb] Output Channel 'IqLim'

! 330:[pgb] Output Channel 'IdLim'

! 340:[pgb] Output Channel 'Iq_ref_pu'

! 350:[pgb] Output Channel 'Id_max'

! 360:[pgb] Output Channel 'Id_ref_pu'

! 370:[pgb] Output Channel 'Iq_max'

      RETURN
      END

!=======================================================================

      SUBROUTINE Current_LimiterOut_Begin()

!---------------------------------------
! Standard includes 
!---------------------------------------

      INCLUDE 'nd.h'
      INCLUDE 'emtconst.h'
      INCLUDE 's0.h'
      INCLUDE 's1.h'
      INCLUDE 's4.h'
      INCLUDE 'branches.h'
      INCLUDE 'pscadv3.h'
      INCLUDE 'radiolinks.h'
      INCLUDE 'rtconfig.h'

!---------------------------------------
! Function/Subroutine Declarations 
!---------------------------------------


!---------------------------------------
! Variable Declarations 
!---------------------------------------


! Subroutine Arguments

! Electrical Node Indices

! Control Signals

! Internal Variables

! Indexing variables
      INTEGER ICALL_NO                            ! Module call num
      INTEGER SS                                  ! SS/Node/Branch/Xfmr


!---------------------------------------
! Local Indices 
!---------------------------------------


! Define electric network subsystem number

      SS        = NODE(NNODE+1)

! Increment and assign runtime configuration call indices

      ICALL_NO  = NCALL_NO
      NCALL_NO  = NCALL_NO + 1

! Increment global storage indices

      NNODE     = NNODE + 2
      NCSCS     = NCSCS + 0
      NCSCR     = NCSCR + 0

!---------------------------------------
! Electrical Node Lookup 
!---------------------------------------


!---------------------------------------
! Generated code from module definition 
!---------------------------------------


      RETURN
      END

